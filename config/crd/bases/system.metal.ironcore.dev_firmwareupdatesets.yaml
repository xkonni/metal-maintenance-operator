---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firmwareupdatesets.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: FirmwareUpdateSet
    listKind: FirmwareUpdateSetList
    plural: firmwareupdatesets
    shortNames:
    - fwus
    singular: firmwareupdateset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.matchingServers
      name: MatchingServers
      type: integer
    - jsonPath: .status.pendingFirmwareUpdate
      name: Pending
      type: integer
    - jsonPath: .status.inProgressFirmwareUpdate
      name: InProgress
      type: integer
    - jsonPath: .status.completedFirmwareUpdate
      name: Completed
      type: integer
    - jsonPath: .status.failedFirmwareUpdate
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareUpdateSet is the Schema for the firmwareupdatesets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirmwareUpdateSetSpec defines the desired state of FirmwareUpdateSet.
            properties:
              firmwareUpdateTemplate:
                description: FirmwareUpdateTemplate defines the template for the FirmwareUpdate
                  resource to be applied to the servers.
                properties:
                  image:
                    description: Image describes the OTB firmware image parameters
                      (HPE, Lenovo).
                    properties:
                      URI:
                        description: URI is the URI of the software image to install.
                        type: string
                      secretRef:
                        description: SecretRef is a reference to the Secret containing
                          the credentials to access the image URI.
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      transferProtocol:
                        description: TransferProtocol is the network protocol used
                          to retrieve the image URI.
                        type: string
                    required:
                    - URI
                    type: object
                  repository:
                    description: Repository describes the network share hosting the
                      Dell update repository/catalog.
                    properties:
                      address:
                        description: Address is the share's hostname or IP address
                          (e.g. downloads.dell.com).
                        type: string
                      applyDowngradeVersions:
                        description: ApplyDowngradeVersions, if true, allows applying
                          packages older than the currently installed version.
                        type: boolean
                      applySameVersions:
                        description: ApplySameVersions, if true, re-applies packages
                          already at the same version.
                        type: boolean
                      catalogFile:
                        description: CatalogFile is the catalog file name within the
                          share. Defaults to "Catalog.xml".
                        type: string
                      credentialsRef:
                        description: CredentialsRef references the credentials used
                          to authenticate against the share, if required.
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      ignoreCertWarning:
                        description: IgnoreCertWarning, if true, ignores certificate
                          warnings for HTTPS shares.
                        type: boolean
                      rebootNeeded:
                        description: RebootNeeded, if true, allows the BMC to reboot
                          the server to apply updates.
                        type: boolean
                      shareName:
                        description: ShareName is the network share name. Not required
                          for HTTP/HTTPS catalogs.
                        type: string
                      shareType:
                        description: ShareType is the type of network share hosting
                          the repository.
                        enum:
                        - NFS
                        - CIFS
                        - HTTP
                        - HTTPS
                        type: string
                      workgroup:
                        description: Workgroup is the CIFS workgroup, if applicable.
                        type: string
                    required:
                    - shareType
                    type: object
                  retryPolicy:
                    description: RetryPolicy defines the retry behavior for automatic
                      retries on transient failures.
                    properties:
                      maxAttempts:
                        description: |-
                          MaxAttempts is the maximum number of automatic retry attempts after failure.
                          0 means no automatic retries. Must be between 0 and 10 inclusive.
                          If not set, the operator-level default is used.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                    type: object
                  serverMaintenancePolicy:
                    description: ServerMaintenancePolicy is a maintenance policy to
                      be enforced on the server.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: exactly one of repository or image must be set
                  rule: has(self.repository) != has(self.image)
              serverSelector:
                description: ServerSelector specifies a label selector to identify
                  the servers to apply the firmware update on.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              updateStrategy:
                description: UpdateStrategy controls how FirmwareUpdate children are
                  rolled out.
                properties:
                  maxConcurrent:
                    description: |-
                      MaxConcurrent limits the number of FirmwareUpdate children in InProgress state at once.
                      Zero means unlimited.
                    format: int32
                    type: integer
                type: object
            required:
            - serverSelector
            type: object
          status:
            description: FirmwareUpdateSetStatus defines the observed state of FirmwareUpdateSet.
            properties:
              completedFirmwareUpdate:
                description: CompletedFirmwareUpdate is the number of FirmwareUpdate
                  resources that completed successfully.
                format: int32
                type: integer
              failedFirmwareUpdate:
                description: FailedFirmwareUpdate is the number of FirmwareUpdate
                  resources that failed.
                format: int32
                type: integer
              inProgressFirmwareUpdate:
                description: InProgressFirmwareUpdate is the number of FirmwareUpdate
                  resources currently in progress.
                format: int32
                type: integer
              matchingServers:
                description: MatchingServers is the number of servers matching the
                  selector.
                format: int32
                type: integer
              pendingFirmwareUpdate:
                description: PendingFirmwareUpdate is the number of FirmwareUpdate
                  resources in a pending state.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

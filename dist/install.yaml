apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
    control-plane: controller-manager
  name: metal-maintenance-operator-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: biossettings.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: BIOSSettings
    listKind: BIOSSettingsList
    plural: biossettings
    shortNames:
    - bioss
    singular: biossettings
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: BIOSVersion
      type: string
    - jsonPath: .spec.serverRef.name
      name: ServerRef
      type: string
    - jsonPath: .spec.serverMaintenanceRef.name
      name: ServerMaintenanceRef
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.lastAppliedTime
      name: AppliedOn
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BIOSSettings is the Schema for the biossettings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BIOSSettingsSpec defines the desired state of BIOSSettings.
            properties:
              retryPolicy:
                description: RetryPolicy defines the retry behavior for automatic
                  retries on transient failures.
                properties:
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of automatic retry attempts after failure.
                      0 means no automatic retries. Must be between 0 and 10 inclusive.
                      If not set, the operator-level default is used.
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                type: object
              serverMaintenancePolicy:
                description: ServerMaintenancePolicy is a maintenance policy to be
                  applied on the server.
                type: string
              serverMaintenanceRef:
                description: ServerMaintenanceRef is a reference to a ServerMaintenance
                  object that BIOSSettings has requested for the referred server.
                properties:
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced object.
                    type: string
                required:
                - name
                - namespace
                type: object
              serverRef:
                description: ServerRef is a reference to a specific server to apply
                  the BIOS settings on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef is immutable
                  rule: self == oldSelf
              settingsFlow:
                description: SettingsFlow contains the settings sequence to apply
                  in the given order.
                items:
                  properties:
                    name:
                      description: Name is the name of the flow item.
                      maxLength: 1000
                      minLength: 1
                      type: string
                    priority:
                      description: Priority defines the order of applying the settings.
                        Lower numbers have higher priority (i.e. lower numbers are
                        applied first).
                      format: int32
                      maximum: 2147483645
                      minimum: 1
                      type: integer
                    settings:
                      additionalProperties:
                        type: string
                      description: Settings contains software (e.g. BIOS, BMC) settings
                        as a map.
                      type: object
                  required:
                  - name
                  - priority
                  type: object
                type: array
              variables:
                description: Variables is a list of variables that can be used in
                  the settings for templating.
                items:
                  properties:
                    key:
                      description: Key is the name of the variable to be used in the
                        BMCSettingsTemplate format.
                      maxLength: 63
                      minLength: 1
                      type: string
                    valueFrom:
                      description: ValueFrom defines a simple single source for the
                        variable value.
                      properties:
                        configMapKeyRef:
                          description: ConfigMapKeyRef points to a namespaced ConfigMap
                            key.
                          properties:
                            key:
                              description: Key is the key within the referenced object.
                              maxLength: 253
                              minLength: 1
                              type: string
                            name:
                              description: Name is the referenced object name.
                              maxLength: 253
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace is the referenced object namespace.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        fieldRef:
                          description: |-
                            FieldRef sources the value from a field of the BMCSettings object itself (e.g. spec.bmcRef.name).
                            Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                          properties:
                            fieldPath:
                              description: |-
                                FieldPath is the path of the field on the BMCSettings object to select (e.g. spec.bmcRef.name).
                                Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                              maxLength: 256
                              minLength: 1
                              type: string
                          required:
                          - fieldPath
                          type: object
                        objectFieldRef:
                          description: |-
                            ObjectFieldRef sources the value from a field of a named related object.
                            The kind must be "BMC". Supports dot-path navigation and bracket notation for map keys
                            containing dots or slashes (e.g. metadata.labels[kubernetes.metal.cloud.sap/nodename]).
                          properties:
                            fieldPath:
                              description: |-
                                FieldPath is the path of the field to select on the target object.
                                Supports dot-path navigation (e.g. metadata.name) and bracket notation for map
                                keys containing dots or slashes (e.g. metadata.labels[topology.kubernetes.io/region]).
                              maxLength: 256
                              minLength: 1
                              type: string
                            kind:
                              description: Kind is the API kind of the object to read
                                the field from (e.g. "BMC").
                              maxLength: 63
                              minLength: 1
                              type: string
                            name:
                              description: |-
                                Name is the name of the object to read the field from.
                                Supports $(VAR) substitution using previously resolved variables in declaration order.
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - fieldPath
                          - kind
                          - name
                          type: object
                        secretKeyRef:
                          description: SecretKeyRef points to a namespaced Secret
                            key.
                          properties:
                            key:
                              description: Key is the key within the referenced object.
                              maxLength: 253
                              minLength: 1
                              type: string
                            name:
                              description: Name is the referenced object name.
                              maxLength: 253
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace is the referenced object namespace.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of fieldRef, objectFieldRef, configMapKeyRef,
                          or secretKeyRef must be provided
                        rule: '(has(self.fieldRef) ? 1 : 0) + (has(self.objectFieldRef)
                          ? 1 : 0) + (has(self.configMapKeyRef) ? 1 : 0) + (has(self.secretKeyRef)
                          ? 1 : 0) == 1'
                      - message: objectFieldRef.kind must be 'BMC'
                        rule: '!has(self.objectFieldRef) || self.objectFieldRef.kind
                          == ''BMC'''
                  required:
                  - key
                  - valueFrom
                  type: object
                maxItems: 64
                type: array
              version:
                description: Version specifies the software version (e.g. BIOS, BMC)
                  these settings apply to.
                type: string
            required:
            - serverRef
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.version) > 0
          status:
            description: BIOSSettingsStatus defines the observed state of BIOSSettings.
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the BIOSSettings's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedAttempts:
                description: FailedAttempts is the number of automatic retry attempts
                  made after failure.
                format: int32
                type: integer
              flowState:
                description: FlowState is a list of individual BIOSSettings operation
                  flows.
                items:
                  description: BIOSSettingsFlowStatus describes the per-priority-step
                    reconciliation state.
                  properties:
                    conditions:
                      description: Conditions represents the latest available observations
                        of the BIOSSettings's current Flowstate.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                    flowState:
                      description: State represents the current state of the BIOS
                        settings update for the current priority.
                      type: string
                    lastAppliedTime:
                      description: LastAppliedTime represents the timestamp when the
                        last setting was successfully applied.
                      format: date-time
                      type: string
                    name:
                      description: Name identifies the current priority settings from
                        the spec.
                      type: string
                    priority:
                      description: Priority identifies the settings priority from
                        the spec.
                      format: int32
                      type: integer
                  type: object
                type: array
              lastAppliedTime:
                description: LastAppliedTime represents the timestamp when the last
                  setting was successfully applied.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              state:
                description: State represents the current state of the BIOS settings
                  update.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: biossettingssets.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: BIOSSettingsSet
    listKind: BIOSSettingsSetList
    plural: biossettingssets
    shortNames:
    - biosss
    singular: biossettingsset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.biosSettingsTemplate.version
      name: BIOSVersion
      type: string
    - jsonPath: .status.fullyLabeledServers
      name: TotalServers
      type: string
    - jsonPath: .status.availableBIOSSettings
      name: AvailableBIOSSettings
      type: string
    - jsonPath: .status.pendingBIOSSettings
      name: Pending
      type: string
    - jsonPath: .status.inProgressBIOSSettings
      name: InProgress
      type: string
    - jsonPath: .status.completedBIOSSettings
      name: Completed
      type: string
    - jsonPath: .status.failedBIOSSettings
      name: Failed
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BIOSSettingsSet is the Schema for the biossettingssets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BIOSSettingsSetSpec defines the desired state of BIOSSettingsSet.
            properties:
              biosSettingsTemplate:
                description: BIOSSettingsTemplate defines the template for the BIOSSettings
                  resource to be applied to the servers.
                properties:
                  retryPolicy:
                    description: RetryPolicy defines the retry behavior for automatic
                      retries on transient failures.
                    properties:
                      maxAttempts:
                        description: |-
                          MaxAttempts is the maximum number of automatic retry attempts after failure.
                          0 means no automatic retries. Must be between 0 and 10 inclusive.
                          If not set, the operator-level default is used.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                    type: object
                  serverMaintenancePolicy:
                    description: ServerMaintenancePolicy is a maintenance policy to
                      be applied on the server.
                    type: string
                  settingsFlow:
                    description: SettingsFlow contains the settings sequence to apply
                      in the given order.
                    items:
                      properties:
                        name:
                          description: Name is the name of the flow item.
                          maxLength: 1000
                          minLength: 1
                          type: string
                        priority:
                          description: Priority defines the order of applying the
                            settings. Lower numbers have higher priority (i.e. lower
                            numbers are applied first).
                          format: int32
                          maximum: 2147483645
                          minimum: 1
                          type: integer
                        settings:
                          additionalProperties:
                            type: string
                          description: Settings contains software (e.g. BIOS, BMC)
                            settings as a map.
                          type: object
                      required:
                      - name
                      - priority
                      type: object
                    type: array
                  variables:
                    description: Variables is a list of variables that can be used
                      in the settings for templating.
                    items:
                      properties:
                        key:
                          description: Key is the name of the variable to be used
                            in the BMCSettingsTemplate format.
                          maxLength: 63
                          minLength: 1
                          type: string
                        valueFrom:
                          description: ValueFrom defines a simple single source for
                            the variable value.
                          properties:
                            configMapKeyRef:
                              description: ConfigMapKeyRef points to a namespaced
                                ConfigMap key.
                              properties:
                                key:
                                  description: Key is the key within the referenced
                                    object.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                name:
                                  description: Name is the referenced object name.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace is the referenced object
                                    namespace.
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              - namespace
                              type: object
                            fieldRef:
                              description: |-
                                FieldRef sources the value from a field of the BMCSettings object itself (e.g. spec.bmcRef.name).
                                Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                              properties:
                                fieldPath:
                                  description: |-
                                    FieldPath is the path of the field on the BMCSettings object to select (e.g. spec.bmcRef.name).
                                    Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                                  maxLength: 256
                                  minLength: 1
                                  type: string
                              required:
                              - fieldPath
                              type: object
                            objectFieldRef:
                              description: |-
                                ObjectFieldRef sources the value from a field of a named related object.
                                The kind must be "BMC". Supports dot-path navigation and bracket notation for map keys
                                containing dots or slashes (e.g. metadata.labels[kubernetes.metal.cloud.sap/nodename]).
                              properties:
                                fieldPath:
                                  description: |-
                                    FieldPath is the path of the field to select on the target object.
                                    Supports dot-path navigation (e.g. metadata.name) and bracket notation for map
                                    keys containing dots or slashes (e.g. metadata.labels[topology.kubernetes.io/region]).
                                  maxLength: 256
                                  minLength: 1
                                  type: string
                                kind:
                                  description: Kind is the API kind of the object
                                    to read the field from (e.g. "BMC").
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                                name:
                                  description: |-
                                    Name is the name of the object to read the field from.
                                    Supports $(VAR) substitution using previously resolved variables in declaration order.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                              required:
                              - fieldPath
                              - kind
                              - name
                              type: object
                            secretKeyRef:
                              description: SecretKeyRef points to a namespaced Secret
                                key.
                              properties:
                                key:
                                  description: Key is the key within the referenced
                                    object.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                name:
                                  description: Name is the referenced object name.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace is the referenced object
                                    namespace.
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              - namespace
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of fieldRef, objectFieldRef, configMapKeyRef,
                              or secretKeyRef must be provided
                            rule: '(has(self.fieldRef) ? 1 : 0) + (has(self.objectFieldRef)
                              ? 1 : 0) + (has(self.configMapKeyRef) ? 1 : 0) + (has(self.secretKeyRef)
                              ? 1 : 0) == 1'
                          - message: objectFieldRef.kind must be 'BMC'
                            rule: '!has(self.objectFieldRef) || self.objectFieldRef.kind
                              == ''BMC'''
                      required:
                      - key
                      - valueFrom
                      type: object
                    maxItems: 64
                    type: array
                  version:
                    description: Version specifies the software version (e.g. BIOS,
                      BMC) these settings apply to.
                    type: string
                type: object
              serverSelector:
                description: ServerSelector specifies a label selector to identify
                  the servers that are to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - biosSettingsTemplate
            - serverSelector
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.biosSettingsTemplate.version) > 0
          status:
            description: BIOSSettingsSetStatus defines the observed state of BIOSSettingsSet.
            properties:
              availableBIOSSettings:
                description: AvailableBIOSSettings is the number of BIOSSettings currently
                  created by the set.
                format: int32
                type: integer
              completedBIOSSettings:
                description: CompletedBIOSSettings is the total number of completed
                  BIOSSettings in the set.
                format: int32
                type: integer
              failedBIOSSettings:
                description: FailedBIOSSettings is the total number of failed BIOSSettings
                  in the set.
                format: int32
                type: integer
              fullyLabeledServers:
                description: FullyLabeledServers is the number of servers in the set.
                format: int32
                type: integer
              inProgressBIOSSettings:
                description: InProgressBIOSSettings is the total number of BIOSSettings
                  in the set that are currently in progress.
                format: int32
                type: integer
              pendingBIOSSettings:
                description: PendingBIOSSettings is the total number of pending BIOSSettings
                  in the set.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: biosversions.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: BIOSVersion
    listKind: BIOSVersionList
    plural: biosversions
    shortNames:
    - biosv
    singular: biosversion
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: BIOSVersion
      type: string
    - jsonPath: .spec.updatePolicy
      name: UpdatePolicy
      type: string
    - jsonPath: .spec.serverRef.name
      name: ServerRef
      type: string
    - jsonPath: .spec.serverMaintenanceRef.name
      name: ServerMaintenanceRef
      type: string
    - jsonPath: .status.upgradeTask.state
      name: TaskState
      type: string
    - jsonPath: .status.upgradeTask.status
      name: TaskStatus
      type: string
    - jsonPath: .status.upgradeTask.percentageComplete
      name: TaskProgress
      type: integer
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BIOSVersion is the Schema for the biosversions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BIOSVersionSpec defines the desired state of BIOSVersion.
            properties:
              image:
                description: Image specifies the image to use to upgrade to the given
                  BIOS version.
                properties:
                  URI:
                    description: URI is the URI of the software image to install.
                    type: string
                  secretRef:
                    description: SecretRef is a reference to the Secret containing
                      the credentials to access the image URI.
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  transferProtocol:
                    description: TransferProtocol is the network protocol used to
                      retrieve the image URI.
                    type: string
                required:
                - URI
                type: object
              retryPolicy:
                description: RetryPolicy defines the retry behavior for automatic
                  retries on transient failures.
                properties:
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of automatic retry attempts after failure.
                      0 means no automatic retries. Must be between 0 and 10 inclusive.
                      If not set, the operator-level default is used.
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                type: object
              serverMaintenancePolicy:
                description: ServerMaintenancePolicy is a maintenance policy to be
                  enforced on the server.
                type: string
              serverMaintenanceRef:
                description: ServerMaintenanceRef is a reference to a ServerMaintenance
                  object that the controller has requested for the referred server.
                properties:
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced object.
                    type: string
                required:
                - name
                - namespace
                type: object
              serverRef:
                description: ServerRef is a reference to a specific server to apply
                  the BIOS upgrade on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef is immutable
                  rule: self == oldSelf
              updatePolicy:
                description: UpdatePolicy indicates whether the server's upgrade service
                  should bypass vendor update policies.
                type: string
              version:
                description: Version specifies the BIOS version to upgrade to.
                type: string
            required:
            - image
            - serverRef
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.version) > 0
          status:
            description: BIOSVersionStatus defines the observed state of BIOSVersion.
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the BIOS version upgrade state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedAttempts:
                description: FailedAttempts is the number of automatic retry attempts
                  made after failure.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              state:
                description: State represents the current state of the BIOS upgrade
                  task.
                type: string
              upgradeTask:
                description: UpgradeTask contains the state of the Upgrade Task created
                  by the BMC.
                properties:
                  URI:
                    description: URI is the URI of the task created by the BMC for
                      the BIOS upgrade.
                    type: string
                  percentageComplete:
                    description: PercentComplete is the percentage of completion of
                      the task.
                    format: int32
                    type: integer
                  state:
                    description: State is the current state of the task.
                    type: string
                  status:
                    description: Status is the current status of the task.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: biosversionsets.system.metal.ironcore.dev
spec:
  group: system.metal.ironcore.dev
  names:
    kind: BIOSVersionSet
    listKind: BIOSVersionSetList
    plural: biosversionsets
    shortNames:
    - biosvs
    singular: biosversionset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.biosVersionTemplate.version
      name: BIOSVersion
      type: string
    - jsonPath: .status.fullyLabeledServers
      name: selectedServers
      type: integer
    - jsonPath: .status.availableBIOSVersion
      name: AvailableBIOSVersion
      type: integer
    - jsonPath: .status.pendingBIOSVersion
      name: Pending
      type: integer
    - jsonPath: .status.inProgressBIOSVersion
      name: InProgress
      type: integer
    - jsonPath: .status.completedBIOSVersion
      name: Completed
      type: integer
    - jsonPath: .status.failedBIOSVersion
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BIOSVersionSet is the Schema for the biosversionsets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BIOSVersionSetSpec defines the desired state of BIOSVersionSet.
            properties:
              biosVersionTemplate:
                description: BIOSVersionTemplate defines the template for the BIOSVersion
                  resource to be applied to the servers.
                properties:
                  image:
                    description: Image specifies the image to use to upgrade to the
                      given BIOS version.
                    properties:
                      URI:
                        description: URI is the URI of the software image to install.
                        type: string
                      secretRef:
                        description: SecretRef is a reference to the Secret containing
                          the credentials to access the image URI.
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      transferProtocol:
                        description: TransferProtocol is the network protocol used
                          to retrieve the image URI.
                        type: string
                    required:
                    - URI
                    type: object
                  retryPolicy:
                    description: RetryPolicy defines the retry behavior for automatic
                      retries on transient failures.
                    properties:
                      maxAttempts:
                        description: |-
                          MaxAttempts is the maximum number of automatic retry attempts after failure.
                          0 means no automatic retries. Must be between 0 and 10 inclusive.
                          If not set, the operator-level default is used.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                    type: object
                  serverMaintenancePolicy:
                    description: ServerMaintenancePolicy is a maintenance policy to
                      be enforced on the server.
                    type: string
                  updatePolicy:
                    description: UpdatePolicy indicates whether the server's upgrade
                      service should bypass vendor update policies.
                    type: string
                  version:
                    description: Version specifies the BIOS version to upgrade to.
                    type: string
                required:
                - image
                type: object
              serverSelector:
                description: ServerSelector specifies a label selector to identify
                  the servers that are to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - biosVersionTemplate
            - serverSelector
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.biosVersionTemplate.version) > 0
          status:
            description: BIOSVersionSetStatus defines the observed state of BIOSVersionSet.
            properties:
              availableBIOSVersion:
                description: AvailableBIOSVersion is the number of BIOSVersion created
                  by the set.
                format: int32
                type: integer
              completedBIOSVersion:
                description: CompletedBIOSVersion is the total number of completed
                  BIOSVersion in the set.
                format: int32
                type: integer
              failedBIOSVersion:
                description: FailedBIOSVersion is the total number of failed BIOSVersion
                  in the set.
                format: int32
                type: integer
              fullyLabeledServers:
                description: FullyLabeledServers is the number of servers in the set.
                format: int32
                type: integer
              inProgressBIOSVersion:
                description: InProgressBIOSVersion is the total number of BIOSVersion
                  resources in the set that are currently in progress.
                format: int32
                type: integer
              pendingBIOSVersion:
                description: PendingBIOSVersion is the total number of pending BIOSVersion
                  in the set.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bmcsettings.baseboard.metal.ironcore.dev
spec:
  group: baseboard.metal.ironcore.dev
  names:
    kind: BMCSettings
    listKind: BMCSettingsList
    plural: bmcsettings
    singular: bmcsettings
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: BMCVersion
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.bmcRef.name
      name: BMCRef
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BMCSettings is the Schema for the BMCSettings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BMCSettingsSpec defines the desired state of BMCSettings.
            properties:
              bmcRef:
                description: BMCRef is a reference to a specific BMC to apply settings
                  to.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: bmcRef is immutable
                  rule: self == oldSelf
              retryPolicy:
                description: RetryPolicy defines the retry behavior for automatic
                  retries on transient failures.
                properties:
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of automatic retry attempts after failure.
                      0 means no automatic retries. Must be between 0 and 10 inclusive.
                      If not set, the operator-level default is used.
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                type: object
              serverMaintenancePolicy:
                description: ServerMaintenancePolicy is a maintenance policy to be
                  applied on the server.
                type: string
              serverMaintenanceRefs:
                description: |-
                  ServerMaintenanceRefs are references to ServerMaintenance objects which are created by the controller for each
                  server that needs to be updated with the BMC settings.
                items:
                  description: ServerMaintenanceRefItem is a reference to a ServerMaintenance
                    object.
                  properties:
                    serverMaintenanceRef:
                      description: ServerMaintenanceRef is a reference to a ServerMaintenance
                        object that the BMCSettings has requested for the referred
                        server.
                      properties:
                        name:
                          description: Name is the name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace is the namespace of the referenced
                            object.
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                  type: object
                type: array
              settingsFlow:
                description: SettingsFlow contains the settings sequence to apply
                  in the given order.
                items:
                  properties:
                    name:
                      description: Name is the name of the flow item.
                      maxLength: 1000
                      minLength: 1
                      type: string
                    priority:
                      description: Priority defines the order of applying the settings.
                        Lower numbers have higher priority (i.e. lower numbers are
                        applied first).
                      format: int32
                      maximum: 2147483645
                      minimum: 1
                      type: integer
                    settings:
                      additionalProperties:
                        type: string
                      description: Settings contains software (e.g. BIOS, BMC) settings
                        as a map.
                      type: object
                  required:
                  - name
                  - priority
                  type: object
                type: array
              variables:
                description: Variables is a list of variables that can be used in
                  the settings for templating.
                items:
                  properties:
                    key:
                      description: Key is the name of the variable to be used in the
                        BMCSettingsTemplate format.
                      maxLength: 63
                      minLength: 1
                      type: string
                    valueFrom:
                      description: ValueFrom defines a simple single source for the
                        variable value.
                      properties:
                        configMapKeyRef:
                          description: ConfigMapKeyRef points to a namespaced ConfigMap
                            key.
                          properties:
                            key:
                              description: Key is the key within the referenced object.
                              maxLength: 253
                              minLength: 1
                              type: string
                            name:
                              description: Name is the referenced object name.
                              maxLength: 253
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace is the referenced object namespace.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        fieldRef:
                          description: |-
                            FieldRef sources the value from a field of the BMCSettings object itself (e.g. spec.bmcRef.name).
                            Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                          properties:
                            fieldPath:
                              description: |-
                                FieldPath is the path of the field on the BMCSettings object to select (e.g. spec.bmcRef.name).
                                Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                              maxLength: 256
                              minLength: 1
                              type: string
                          required:
                          - fieldPath
                          type: object
                        objectFieldRef:
                          description: |-
                            ObjectFieldRef sources the value from a field of a named related object.
                            The kind must be "BMC". Supports dot-path navigation and bracket notation for map keys
                            containing dots or slashes (e.g. metadata.labels[kubernetes.metal.cloud.sap/nodename]).
                          properties:
                            fieldPath:
                              description: |-
                                FieldPath is the path of the field to select on the target object.
                                Supports dot-path navigation (e.g. metadata.name) and bracket notation for map
                                keys containing dots or slashes (e.g. metadata.labels[topology.kubernetes.io/region]).
                              maxLength: 256
                              minLength: 1
                              type: string
                            kind:
                              description: Kind is the API kind of the object to read
                                the field from (e.g. "BMC").
                              maxLength: 63
                              minLength: 1
                              type: string
                            name:
                              description: |-
                                Name is the name of the object to read the field from.
                                Supports $(VAR) substitution using previously resolved variables in declaration order.
                              maxLength: 253
                              minLength: 1
                              type: string
                          required:
                          - fieldPath
                          - kind
                          - name
                          type: object
                        secretKeyRef:
                          description: SecretKeyRef points to a namespaced Secret
                            key.
                          properties:
                            key:
                              description: Key is the key within the referenced object.
                              maxLength: 253
                              minLength: 1
                              type: string
                            name:
                              description: Name is the referenced object name.
                              maxLength: 253
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace is the referenced object namespace.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                      x-kubernetes-validations:
                      - message: exactly one of fieldRef, objectFieldRef, configMapKeyRef,
                          or secretKeyRef must be provided
                        rule: '(has(self.fieldRef) ? 1 : 0) + (has(self.objectFieldRef)
                          ? 1 : 0) + (has(self.configMapKeyRef) ? 1 : 0) + (has(self.secretKeyRef)
                          ? 1 : 0) == 1'
                      - message: objectFieldRef.kind must be 'BMC'
                        rule: '!has(self.objectFieldRef) || self.objectFieldRef.kind
                          == ''BMC'''
                  required:
                  - key
                  - valueFrom
                  type: object
                maxItems: 64
                type: array
              version:
                description: Version specifies the software version (e.g. BIOS, BMC)
                  these settings apply to.
                type: string
            required:
            - bmcRef
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.version) > 0
            - message: variable keys must be unique
              rule: '!has(self.variables) || self.variables.all(v, self.variables.filter(w,
                w.key == v.key).size() == 1)'
          status:
            description: BMCSettingsStatus defines the observed state of BMCSettings.
            properties:
              appliedETags:
                additionalProperties:
                  description: |-
                    BMCSettingsApplyResultEntry holds the URI, ETag, and value hash from the last
                    successful apply of a single settings key. Used for ETag-based drift detection.
                  properties:
                    etag:
                      description: |-
                        ETag is the drift-detection token captured after the last successful apply.
                        Either a real ETag returned by the BMC (e.g. W/"20B77DA6") or a SHA-256
                        hash of the GET response body prefixed with "hash:sha256:" for BMCs that
                        do not return ETag headers.
                      type: string
                    uri:
                      description: |-
                        URI is the Redfish resource URI from the apply response.
                        For PATCH operations this is the request URI; for POST operations this is
                        the Location header value pointing to the created resource.
                      type: string
                    valueHash:
                      description: |-
                        ValueHash is the SHA-256 hash of the effective (resolved) value at apply time.
                        Used to detect desired-state changes from ConfigMap/Secret rotation independent
                        of BMC-side drift.
                      type: string
                  type: object
                description: |-
                  AppliedETags stores the URI, ETag, and value hash from the last successful
                  apply for each settings key. Used for ETag-based drift detection during
                  verification to avoid unnecessary re-applies and to reliably detect
                  write-only field drift (e.g. passwords) and POST-created resource drift
                  (e.g. certificates).
                type: object
              conditions:
                description: Conditions represents the latest available observations
                  of the BMC Settings Resource state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedAttempts:
                description: FailedAttempts is the number of automatic retry attempts
                  made after failure.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              state:
                description: State represents the current state of the BMC configuration
                  task.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bmcsettingssets.baseboard.metal.ironcore.dev
spec:
  group: baseboard.metal.ironcore.dev
  names:
    kind: BMCSettingsSet
    listKind: BMCSettingsSetList
    plural: bmcsettingssets
    singular: bmcsettingsset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bmcSettingsTemplate.version
      name: BMCVersion
      type: string
    - jsonPath: .status.fullyLabeledBMCs
      name: TotalBMCs
      type: integer
    - jsonPath: .status.availableBMCSettings
      name: AvailableBMCSettings
      type: integer
    - jsonPath: .status.pendingBMCSettings
      name: Pending
      type: integer
    - jsonPath: .status.inProgressBMCSettings
      name: InProgress
      type: integer
    - jsonPath: .status.completedBMCSettings
      name: Completed
      type: integer
    - jsonPath: .status.failedBMCSettings
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BMCSettingsSet is the Schema for the bmcsettingssets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BMCSettingsSetSpec defines the desired state of BMCSettingsSet.
            properties:
              bmcSelector:
                description: BMCSelector specifies a label selector to identify the
                  BMCs to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              bmcSettingsTemplate:
                description: BMCSettingsTemplate defines the template for the BMCSettings
                  resource to be applied to the BMCs.
                properties:
                  retryPolicy:
                    description: RetryPolicy defines the retry behavior for automatic
                      retries on transient failures.
                    properties:
                      maxAttempts:
                        description: |-
                          MaxAttempts is the maximum number of automatic retry attempts after failure.
                          0 means no automatic retries. Must be between 0 and 10 inclusive.
                          If not set, the operator-level default is used.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                    type: object
                  serverMaintenancePolicy:
                    description: ServerMaintenancePolicy is a maintenance policy to
                      be applied on the server.
                    type: string
                  settingsFlow:
                    description: SettingsFlow contains the settings sequence to apply
                      in the given order.
                    items:
                      properties:
                        name:
                          description: Name is the name of the flow item.
                          maxLength: 1000
                          minLength: 1
                          type: string
                        priority:
                          description: Priority defines the order of applying the
                            settings. Lower numbers have higher priority (i.e. lower
                            numbers are applied first).
                          format: int32
                          maximum: 2147483645
                          minimum: 1
                          type: integer
                        settings:
                          additionalProperties:
                            type: string
                          description: Settings contains software (e.g. BIOS, BMC)
                            settings as a map.
                          type: object
                      required:
                      - name
                      - priority
                      type: object
                    type: array
                  variables:
                    description: Variables is a list of variables that can be used
                      in the settings for templating.
                    items:
                      properties:
                        key:
                          description: Key is the name of the variable to be used
                            in the BMCSettingsTemplate format.
                          maxLength: 63
                          minLength: 1
                          type: string
                        valueFrom:
                          description: ValueFrom defines a simple single source for
                            the variable value.
                          properties:
                            configMapKeyRef:
                              description: ConfigMapKeyRef points to a namespaced
                                ConfigMap key.
                              properties:
                                key:
                                  description: Key is the key within the referenced
                                    object.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                name:
                                  description: Name is the referenced object name.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace is the referenced object
                                    namespace.
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              - namespace
                              type: object
                            fieldRef:
                              description: |-
                                FieldRef sources the value from a field of the BMCSettings object itself (e.g. spec.bmcRef.name).
                                Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                              properties:
                                fieldPath:
                                  description: |-
                                    FieldPath is the path of the field on the BMCSettings object to select (e.g. spec.bmcRef.name).
                                    Only string-typed fields are supported; integer, bool, or map fields will cause a resolution error.
                                  maxLength: 256
                                  minLength: 1
                                  type: string
                              required:
                              - fieldPath
                              type: object
                            objectFieldRef:
                              description: |-
                                ObjectFieldRef sources the value from a field of a named related object.
                                The kind must be "BMC". Supports dot-path navigation and bracket notation for map keys
                                containing dots or slashes (e.g. metadata.labels[kubernetes.metal.cloud.sap/nodename]).
                              properties:
                                fieldPath:
                                  description: |-
                                    FieldPath is the path of the field to select on the target object.
                                    Supports dot-path navigation (e.g. metadata.name) and bracket notation for map
                                    keys containing dots or slashes (e.g. metadata.labels[topology.kubernetes.io/region]).
                                  maxLength: 256
                                  minLength: 1
                                  type: string
                                kind:
                                  description: Kind is the API kind of the object
                                    to read the field from (e.g. "BMC").
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                                name:
                                  description: |-
                                    Name is the name of the object to read the field from.
                                    Supports $(VAR) substitution using previously resolved variables in declaration order.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                              required:
                              - fieldPath
                              - kind
                              - name
                              type: object
                            secretKeyRef:
                              description: SecretKeyRef points to a namespaced Secret
                                key.
                              properties:
                                key:
                                  description: Key is the key within the referenced
                                    object.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                name:
                                  description: Name is the referenced object name.
                                  maxLength: 253
                                  minLength: 1
                                  type: string
                                namespace:
                                  description: Namespace is the referenced object
                                    namespace.
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              - namespace
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: exactly one of fieldRef, objectFieldRef, configMapKeyRef,
                              or secretKeyRef must be provided
                            rule: '(has(self.fieldRef) ? 1 : 0) + (has(self.objectFieldRef)
                              ? 1 : 0) + (has(self.configMapKeyRef) ? 1 : 0) + (has(self.secretKeyRef)
                              ? 1 : 0) == 1'
                          - message: objectFieldRef.kind must be 'BMC'
                            rule: '!has(self.objectFieldRef) || self.objectFieldRef.kind
                              == ''BMC'''
                      required:
                      - key
                      - valueFrom
                      type: object
                    maxItems: 64
                    type: array
                  version:
                    description: Version specifies the software version (e.g. BIOS,
                      BMC) these settings apply to.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: variable keys must be unique
                  rule: '!has(self.variables) || self.variables.all(v, self.variables.filter(w,
                    w.key == v.key).size() == 1)'
            required:
            - bmcSelector
            - bmcSettingsTemplate
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.bmcSettingsTemplate.version) > 0
          status:
            description: BMCSettingsSetStatus defines the observed state of BMCSettingsSet.
            properties:
              availableBMCSettings:
                description: AvailableBMCSettings is the number of BMCSettings currently
                  created by the set.
                format: int32
                type: integer
              completedBMCSettings:
                description: CompletedBMCSettings is the total number of completed
                  BMCSettings in the set.
                format: int32
                type: integer
              failedBMCSettings:
                description: FailedBMCSettings is the total number of failed BMCSettings
                  in the set.
                format: int32
                type: integer
              fullyLabeledBMCs:
                description: FullyLabeledBMCs is the number of BMCs in the set.
                format: int32
                type: integer
              inProgressBMCSettings:
                description: InProgressBMCSettings is the total number of BMCSettings
                  in the set that are currently in progress.
                format: int32
                type: integer
              pendingBMCSettings:
                description: PendingBMCSettings is the total number of pending BMCSettings
                  in the set.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bmcusers.baseboard.metal.ironcore.dev
spec:
  group: baseboard.metal.ironcore.dev
  names:
    kind: BMCUser
    listKind: BMCUserList
    plural: bmcusers
    shortNames:
    - bmcu
    singular: bmcuser
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.id
      name: ID
      type: string
    - jsonPath: .spec.userName
      name: UserName
      type: string
    - jsonPath: .spec.roleID
      name: RoleID
      type: string
    - jsonPath: .status.lastRotation
      name: LastRotation
      type: date
    - jsonPath: .status.passwordExpiration
      name: PasswordExpiration
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BMCUser is the Schema for the bmcusers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BMCUserSpec defines the desired state of BMCUser.
            properties:
              bmcRef:
                description: BMCRef references the BMC this user should be created
                  on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              bmcSecretRef:
                description: |-
                  BMCSecretRef references the BMCSecret containing the credentials for this user.
                  If not set, the operator will generate a secure password based on BMC manufacturer requirements.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              description:
                description: Description is a description for the BMC user.
                type: string
              roleID:
                description: RoleID is the ID of the role to assign to the user.
                type: string
              rotationPeriod:
                description: |-
                  RotationPeriod defines how often the password should be rotated.
                  If not set, the password will not be rotated.
                type: string
              userName:
                description: UserName is the username of the BMC user.
                type: string
            required:
            - roleID
            - userName
            type: object
          status:
            description: BMCUserStatus defines the observed state of BMCUser.
            properties:
              conditions:
                description: Conditions reflects the current state of the BMCUser.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              effectiveBMCSecretRef:
                description: |-
                  EffectiveBMCSecretRef references the BMCSecret currently used for this user.
                  This may differ from Spec.BMCSecretRef if the operator generated a password.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              id:
                description: ID is the identifier of the user in the BMC system.
                type: string
              lastRotation:
                description: LastRotation is the timestamp of the last password rotation.
                format: date-time
                type: string
              passwordExpiration:
                description: PasswordExpiration is the timestamp when the password
                  will expire.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bmcversions.baseboard.metal.ironcore.dev
spec:
  group: baseboard.metal.ironcore.dev
  names:
    kind: BMCVersion
    listKind: BMCVersionList
    plural: bmcversions
    shortNames:
    - bmcv
    singular: bmcversion
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: BMCVersion
      type: string
    - jsonPath: .spec.updatePolicy
      name: UpdatePolicy
      type: string
    - jsonPath: .spec.bmcRef.name
      name: BMCRef
      type: string
    - jsonPath: .status.upgradeTask.percentageComplete
      name: TaskProgress
      type: integer
    - jsonPath: .status.upgradeTask.state
      name: TaskState
      type: string
    - jsonPath: .status.upgradeTask.status
      name: TaskStatus
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BMCVersion is the Schema for the bmcversions API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BMCVersionSpec defines the desired state of BMCVersion.
            properties:
              bmcRef:
                description: BMCRef is a reference to a specific BMC to apply BMC
                  upgrade on.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: bmcRef is immutable
                  rule: self == oldSelf
              image:
                description: Image specifies the image to use to upgrade to the given
                  BMC version.
                properties:
                  URI:
                    description: URI is the URI of the software image to install.
                    type: string
                  secretRef:
                    description: SecretRef is a reference to the Secret containing
                      the credentials to access the image URI.
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  transferProtocol:
                    description: TransferProtocol is the network protocol used to
                      retrieve the image URI.
                    type: string
                required:
                - URI
                type: object
              retryPolicy:
                description: RetryPolicy defines the retry behavior for automatic
                  retries on transient failures.
                properties:
                  maxAttempts:
                    description: |-
                      MaxAttempts is the maximum number of automatic retry attempts after failure.
                      0 means no automatic retries. Must be between 0 and 10 inclusive.
                      If not set, the operator-level default is used.
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                type: object
              serverMaintenancePolicy:
                description: ServerMaintenancePolicy is a maintenance policy to be
                  enforced on the server managed by referred BMC.
                type: string
              serverMaintenanceRefs:
                description: ServerMaintenanceRefs are references to ServerMaintenance
                  objects that the controller has requested for the related servers.
                items:
                  description: ObjectReference is the namespaced name reference to
                    an object.
                  properties:
                    name:
                      description: Name is the name of the referenced object.
                      type: string
                    namespace:
                      description: Namespace is the namespace of the referenced object.
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              updatePolicy:
                description: UpdatePolicy indicates whether the server's upgrade service
                  should bypass vendor update policies.
                type: string
              version:
                description: Version specifies the BMC version to upgrade to.
                type: string
            required:
            - bmcRef
            - image
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.version) > 0
          status:
            description: BMCVersionStatus defines the observed state of BMCVersion.
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the BMC version upgrade state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedAttempts:
                description: FailedAttempts is the number of automatic retry attempts
                  made after failure.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              state:
                description: State represents the current state of the BMC configuration
                  task.
                type: string
              upgradeTask:
                description: UpgradeTask contains the state of the upgrade task created
                  by the BMC.
                properties:
                  URI:
                    description: URI is the URI of the task created by the BMC for
                      the BIOS upgrade.
                    type: string
                  percentageComplete:
                    description: PercentComplete is the percentage of completion of
                      the task.
                    format: int32
                    type: integer
                  state:
                    description: State is the current state of the task.
                    type: string
                  status:
                    description: Status is the current status of the task.
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: bmcversionsets.baseboard.metal.ironcore.dev
spec:
  group: baseboard.metal.ironcore.dev
  names:
    kind: BMCVersionSet
    listKind: BMCVersionSetList
    plural: bmcversionsets
    shortNames:
    - bmcvs
    singular: bmcversionset
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bmcVersionTemplate.version
      name: BMCVersion
      type: string
    - jsonPath: .status.fullyLabeledBMCs
      name: selectedBMCs
      type: integer
    - jsonPath: .status.availableBMCVersion
      name: AvailableBMCVersion
      type: integer
    - jsonPath: .status.pendingBMCVersion
      name: Pending
      type: integer
    - jsonPath: .status.inProgressBMCVersion
      name: InProgress
      type: integer
    - jsonPath: .status.completedBMCVersion
      name: Completed
      type: integer
    - jsonPath: .status.failedBMCVersion
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BMCVersionSet is the Schema for the bmcversionsets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BMCVersionSetSpec defines the desired state of BMCVersionSet.
            properties:
              bmcSelector:
                description: BMCSelector specifies a label selector to identify the
                  BMCs to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              bmcVersionTemplate:
                description: BMCVersionTemplate defines the template for the BMCVersion
                  resource to be applied to the BMCs.
                properties:
                  image:
                    description: Image specifies the image to use to upgrade to the
                      given BMC version.
                    properties:
                      URI:
                        description: URI is the URI of the software image to install.
                        type: string
                      secretRef:
                        description: SecretRef is a reference to the Secret containing
                          the credentials to access the image URI.
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      transferProtocol:
                        description: TransferProtocol is the network protocol used
                          to retrieve the image URI.
                        type: string
                    required:
                    - URI
                    type: object
                  retryPolicy:
                    description: RetryPolicy defines the retry behavior for automatic
                      retries on transient failures.
                    properties:
                      maxAttempts:
                        description: |-
                          MaxAttempts is the maximum number of automatic retry attempts after failure.
                          0 means no automatic retries. Must be between 0 and 10 inclusive.
                          If not set, the operator-level default is used.
                        format: int32
                        maximum: 10
                        minimum: 0
                        type: integer
                    type: object
                  serverMaintenancePolicy:
                    description: ServerMaintenancePolicy is a maintenance policy to
                      be enforced on the server managed by referred BMC.
                    type: string
                  updatePolicy:
                    description: UpdatePolicy indicates whether the server's upgrade
                      service should bypass vendor update policies.
                    type: string
                  version:
                    description: Version specifies the BMC version to upgrade to.
                    type: string
                required:
                - image
                type: object
            required:
            - bmcSelector
            - bmcVersionTemplate
            type: object
            x-kubernetes-validations:
            - message: version is required
              rule: size(self.bmcVersionTemplate.version) > 0
          status:
            description: BMCVersionSetStatus defines the observed state of BMCVersionSet.
            properties:
              availableBMCVersion:
                description: AvailableBMCVersion is the number of BMCVersion resources
                  currently created by the set.
                format: int32
                type: integer
              completedBMCVersion:
                description: CompletedBMCVersion is the total number of completed
                  BMCVersion resources in the set.
                format: int32
                type: integer
              failedBMCVersion:
                description: FailedBMCVersion is the total number of failed BMCVersion
                  resources in the set.
                format: int32
                type: integer
              fullyLabeledBMCs:
                description: FullyLabeledBMCs is the number of BMCs in the set.
                format: int32
                type: integer
              inProgressBMCVersion:
                description: InProgressBMCVersion is the total number of BMCVersion
                  resources in the set that are currently in progress.
                format: int32
                type: integer
              pendingBMCVersion:
                description: PendingBMCVersion is the total number of pending BMCVersion
                  resources in the set.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: consoles.vendorconsole.metal.ironcore.dev
spec:
  group: vendorconsole.metal.ironcore.dev
  names:
    kind: Console
    listKind: ConsoleList
    plural: consoles
    singular: console
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Console is the Schema for the consoles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConsoleSpec defines the desired state of Console.
            properties:
              bmcCredentialSecretRef:
                description: BMCCredentialSecretRef references the secret containing
                  BMC credentials.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              connection:
                description: Connection contains the console endpoint and transport-security
                  settings.
                properties:
                  insecureSkipTLSVerify:
                    default: false
                    description: |-
                      InsecureSkipTLSVerify disables TLS certificate verification when
                      communicating with the management console. This should only be used for
                      consoles that present self-signed or otherwise untrusted certificates.
                    type: boolean
                  url:
                    description: URL is the URL of the server management console.
                    type: string
                required:
                - url
                type: object
              manufacturer:
                description: Manufacturer is the manufacturer of the server management
                  console (e.g., "Dell", "HPE", "Lenovo").
                type: string
              serverSelector:
                description: ServerSelector specifies a label selector to identify
                  the servers that are to be selected.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - connection
            - manufacturer
            - serverSelector
            type: object
          status:
            description: ConsoleStatus defines the observed state of Console.
            properties:
              managedServers:
                description: ManagedServers number of managed servers.
                format: int32
                type: integer
              pendingOperations:
                description: PendingOperations tracks in-flight vendor operations.
                items:
                  description: PendingOperation tracks an in-flight vendor operation.
                  properties:
                    hostname:
                      description: Hostname is the DNS name used for the server in
                        the vendor console.
                      type: string
                    ip:
                      description: IP is the BMC IP address of the server.
                      type: string
                    jobId:
                      description: JobID is the vendor-specific job identifier for
                        tracking.
                      type: string
                    lastChecked:
                      description: LastChecked is when the job status was last polled.
                      format: date-time
                      type: string
                    message:
                      description: Message provides human-readable status information.
                      type: string
                    operationType:
                      description: OperationType is the type of operation (Import
                        or Remove).
                      type: string
                    retryCount:
                      description: RetryCount tracks how many times the operation
                        has been retried.
                      format: int32
                      type: integer
                    serverName:
                      description: ServerName is the name of the Server resource.
                      type: string
                    startTime:
                      description: StartTime is when the operation was initiated.
                      format: date-time
                      type: string
                    status:
                      description: Status is the current status of the operation.
                      type: string
                  required:
                  - hostname
                  - ip
                  - operationType
                  - serverName
                  - startTime
                  - status
                  type: object
                type: array
              totalServers:
                description: TotalServers total number of servers.
                format: int32
                type: integer
              unmanagedServers:
                description: UnmanagedServers number of unmanaged servers.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: servermaintenances.maintenance.metal.ironcore.dev
spec:
  group: maintenance.metal.ironcore.dev
  names:
    kind: ServerMaintenance
    listKind: ServerMaintenanceList
    plural: servermaintenances
    shortNames:
    - sm
    singular: servermaintenance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serverRef.name
      name: Server
      type: string
    - jsonPath: .spec.policy
      name: Policy
      type: string
    - jsonPath: .metadata.annotations.metal\.ironcore\.dev\/maintenance-reason
      name: Reason
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerMaintenance is the Schema for the ServerMaintenance API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerMaintenanceSpec defines the desired state of a ServerMaintenance.
            properties:
              locatorLED:
                description: |-
                  LocatorLED specifies the desired state of the server's locator LED during maintenance.
                  When maintenance ends, the locator LED is turned off.
                type: string
              policy:
                description: Policy specifies the maintenance policy to be enforced
                  on the server.
                enum:
                - OwnerApproval
                - Enforced
                type: string
              priority:
                default: 0
                description: |-
                  Priority determines ordering when multiple ServerMaintenance resources target the same server.
                  Higher values are processed first. If priorities are equal, older resources are processed first.
                  If omitted, priority is treated as 0.
                format: int32
                type: integer
              serverRef:
                description: ServerRef is a reference to the server that is to be
                  maintained.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef.name must not be empty
                  rule: self.name != ''
            required:
            - policy
            - serverRef
            type: object
          status:
            description: ServerMaintenanceStatus defines the observed state of a ServerMaintenance.
            properties:
              state:
                description: State specifies the current state of the server maintenance.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: serverwirings.readiness.metal.ironcore.dev
spec:
  group: readiness.metal.ironcore.dev
  names:
    kind: ServerWiring
    listKind: ServerWiringList
    plural: serverwirings
    singular: serverwiring
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerWiring is the Schema for the serverwirings API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerWiringSpec defines the desired state of ServerWiring.
            properties:
              network:
                description: Network defines the expected network wiring for the server.
                properties:
                  interfaces:
                    description: Interfaces is the list of expected network interfaces,
                      keyed by MAC address.
                    items:
                      description: ExpectedInterface defines the expected state of
                        a server network interface.
                      properties:
                        carrierStatus:
                          description: |-
                            CarrierStatus is the expected operational carrier status (e.g. "up").
                            If omitted, carrier status is not checked.
                          type: string
                        macAddress:
                          description: MACAddress is the MAC address of the interface
                            and acts as the primary key.
                          type: string
                        neighbors:
                          description: |-
                            Neighbors lists the LLDP neighbors that must all be present on this interface.
                            If omitted or empty, neighbor presence is not checked.
                          items:
                            description: ExpectedNeighbor defines an expected LLDP
                              neighbor on a network interface.
                            properties:
                              portID:
                                description: PortID is the LLDP port identifier of
                                  the expected neighbor.
                                type: string
                              systemName:
                                description: SystemName is the LLDP system name of
                                  the expected neighbor (e.g. switch hostname).
                                type: string
                            required:
                            - portID
                            - systemName
                            type: object
                          type: array
                      required:
                      - macAddress
                      type: object
                    type: array
                type: object
              serverRef:
                description: ServerRef references the cluster-scoped Server to validate.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: serverRef.name must not be empty
                  rule: self.name != ''
            required:
            - serverRef
            type: object
          status:
            description: ServerWiringStatus defines the observed state of ServerWiring.
            properties:
              mismatches:
                description: Mismatches lists validation failures for the server.
                items:
                  description: InterfaceMismatch describes a single wiring validation
                    failure on a network interface.
                  properties:
                    macAddress:
                      description: MACAddress is the MAC address of the interface
                        that failed validation.
                      type: string
                    message:
                      description: Message describes the mismatch.
                      type: string
                    reason:
                      description: Reason is a machine-readable token identifying
                        the failure type.
                      type: string
                  required:
                  - macAddress
                  - message
                  type: object
                type: array
              ready:
                default: false
                description: Ready is true when all expected interfaces and neighbors
                  were found.
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-controller-manager
  namespace: metal-maintenance-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-leader-election-role
  namespace: metal-maintenance-operator-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-bmcuser-admin-role
rules:
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcusers
  verbs:
  - '*'
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcusers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-bmcuser-editor-role
rules:
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcusers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcusers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-bmcuser-viewer-role
rules:
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcusers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcusers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-console-admin-role
rules:
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles
  verbs:
  - '*'
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-console-editor-role
rules:
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-console-viewer-role
rules:
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: metal-maintenance-operator-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcsettings
  - bmcsettingssets
  - bmcusers
  - bmcversions
  - bmcversionsets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcsettings/finalizers
  - bmcsettingssets/finalizers
  - bmcusers/finalizers
  - bmcversions/finalizers
  - bmcversionsets/finalizers
  verbs:
  - update
- apiGroups:
  - baseboard.metal.ironcore.dev
  resources:
  - bmcsettings/status
  - bmcsettingssets/status
  - bmcusers/status
  - bmcversions/status
  - bmcversionsets/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - batch
  resources:
  - jobs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances/finalizers
  verbs:
  - update
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - metal.ironcore.dev
  resources:
  - bmcs
  - servers
  verbs:
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - metal.ironcore.dev
  resources:
  - bmcs/finalizers
  verbs:
  - update
- apiGroups:
  - metal.ironcore.dev
  resources:
  - bmcsecrets
  - serverbootconfigurations
  - serverclaims
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - metal.ironcore.dev
  resources:
  - endpoints
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - metal.ironcore.dev
  resources:
  - servers/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - readiness.metal.ironcore.dev
  resources:
  - serverwirings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - readiness.metal.ironcore.dev
  resources:
  - serverwirings/finalizers
  verbs:
  - update
- apiGroups:
  - readiness.metal.ironcore.dev
  resources:
  - serverwirings/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - system.metal.ironcore.dev
  resources:
  - biossettings
  - biossettingssets
  - biosversions
  - biosversionsets
  - firmwareupdates
  - firmwareupdatesets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - system.metal.ironcore.dev
  resources:
  - biossettings/finalizers
  - biossettingssets/finalizers
  - biosversions/finalizers
  - biosversionsets/finalizers
  - firmwareupdates/finalizers
  - firmwareupdatesets/finalizers
  verbs:
  - update
- apiGroups:
  - system.metal.ironcore.dev
  resources:
  - biossettings/status
  - biossettingssets/status
  - biosversions/status
  - biosversionsets/status
  - firmwareupdates/status
  - firmwareupdatesets/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles/finalizers
  verbs:
  - update
- apiGroups:
  - vendorconsole.metal.ironcore.dev
  resources:
  - consoles/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: metal-maintenance-operator-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: metal-maintenance-operator-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  - /sd/bmcs
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-servermaintenance-admin-role
rules:
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances
  verbs:
  - '*'
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-servermaintenance-editor-role
rules:
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-servermaintenance-viewer-role
rules:
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - maintenance.metal.ironcore.dev
  resources:
  - servermaintenances/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-leader-election-rolebinding
  namespace: metal-maintenance-operator-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: metal-maintenance-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: metal-maintenance-operator-controller-manager
  namespace: metal-maintenance-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
  name: metal-maintenance-operator-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: metal-maintenance-operator-manager-role
subjects:
- kind: ServiceAccount
  name: metal-maintenance-operator-controller-manager
  namespace: metal-maintenance-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: metal-maintenance-operator-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: metal-maintenance-operator-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: metal-maintenance-operator-controller-manager
  namespace: metal-maintenance-operator-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
    control-plane: controller-manager
  name: metal-maintenance-operator-controller-manager-metrics-service
  namespace: metal-maintenance-operator-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: metal-maintenance-operator
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
    control-plane: controller-manager
  name: metal-maintenance-operator-controller-manager-telemetry-events
  namespace: metal-maintenance-operator-system
spec:
  ports:
  - name: events
    port: 9092
    protocol: TCP
    targetPort: events
  selector:
    app.kubernetes.io/name: metal-maintenance-operator
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: metal-maintenance-operator
    control-plane: controller-manager
  name: metal-maintenance-operator-controller-manager
  namespace: metal-maintenance-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: metal-maintenance-operator
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: metal-maintenance-operator
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        env:
        - name: FORCE_ENV_IN_CHART
          value: "true"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: controller:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 9092
          name: events
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: metal-maintenance-operator-controller-manager
      terminationGracePeriodSeconds: 10
      volumes: []
